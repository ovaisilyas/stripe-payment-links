<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stripe Payment Links</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-link me-2"></i>
                Stripe Payment Links
            </a>
            <% if (user) { %>
            <div class="navbar-nav ms-auto">
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user me-1"></i>
                        <%= user.name %>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="/payment/create">
                            <i class="fas fa-plus me-2"></i>Create Payment Link
                        </a></li>
                        <li><a class="dropdown-item" href="/payment/links">
                            <i class="fas fa-list me-2"></i>My Links
                        </a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <form action="/auth/logout" method="POST" class="d-inline">
                                <button type="submit" class="dropdown-item">
                                    <i class="fas fa-sign-out-alt me-2"></i>Logout
                                </button>
                            </form>
                        </li>
                    </ul>
                </div>
            </div>
            <% } %>
        </div>
    </nav>

    <main class="container mt-4">
        <!-- Flash Messages -->
        <% if (success && success.length > 0) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle me-2"></i>
                <%= success[0] %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-circle me-2"></i>
                <%= error[0] %>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% } %>

<div class="row">
    <div class="col-lg-8 mx-auto">
        <div class="card shadow">
            <div class="card-header bg-success text-white">
                <h4 class="mb-0">
                    <i class="fas fa-plus-circle me-2"></i>
                    Create Payment Link
                </h4>
            </div>
            <div class="card-body p-4">
                <% if (errors && errors.length > 0) { %>
                    <div class="alert alert-danger">
                        <ul class="mb-0">
                            <% errors.forEach(function(error) { %>
                                <li><%= error %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } %>

                <form action="/payment/create" method="POST" id="paymentForm" novalidate>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="name" class="form-label">
                                    <i class="fas fa-tag me-1"></i>
                                    Product/Service Name *
                                </label>
                                <input 
                                    type="text" 
                                    class="form-control" 
                                    id="name" 
                                    name="name" 
                                    value="<%= formData ? formData.name : '' %>"
                                    required
                                    maxlength="100"
                                    placeholder="e.g., Premium Subscription"
                                >
                                <div class="form-text">Maximum 100 characters</div>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="amount" class="form-label">
                                    <i class="fas fa-dollar-sign me-1"></i>
                                    Amount *
                                </label>
                                <div class="input-group">
                                    <span class="input-group-text">$</span>
                                    <input 
                                        type="number" 
                                        class="form-control" 
                                        id="amount" 
                                        name="amount" 
                                        value="<%= formData ? formData.amount : '' %>"
                                        step="0.01"
                                        min="0.5"
                                        required
                                        placeholder="0.00"
                                    >
                                </div>
                                <div class="form-text">Minimum $0.50</div>
                            </div>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">
                            <i class="fas fa-align-left me-1"></i>
                            Description
                        </label>
                        <textarea 
                            class="form-control" 
                            id="description" 
                            name="description" 
                            rows="3"
                            maxlength="500"
                            placeholder="Optional description of the product or service"
                        ><%= formData ? formData.description : '' %></textarea>
                        <div class="form-text">Maximum 500 characters</div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="currency" class="form-label">
                                    <i class="fas fa-globe me-1"></i>
                                    Currency
                                </label>
                                <select class="form-select" id="currency" name="currency">
                                    <option value="usd" <%= (formData && formData.currency === 'usd') || !formData ? 'selected' : '' %>>USD - US Dollar</option>
                                    <option value="eur" <%= formData && formData.currency === 'eur' ? 'selected' : '' %>>EUR - Euro</option>
                                    <option value="gbp" <%= formData && formData.currency === 'gbp' ? 'selected' : '' %>>GBP - British Pound</option>
                                    <option value="cad" <%= formData && formData.currency === 'cad' ? 'selected' : '' %>>CAD - Canadian Dollar</option>
                                    <option value="aud" <%= formData && formData.currency === 'aud' ? 'selected' : '' %>>AUD - Australian Dollar</option>
                                </select>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="quantity" class="form-label">
                                    <i class="fas fa-hashtag me-1"></i>
                                    Quantity
                                </label>
                                <input 
                                    type="number" 
                                    class="form-control" 
                                    id="quantity" 
                                    name="quantity" 
                                    value="<%= formData ? formData.quantity : '1' %>"
                                    min="1"
                                    max="1000"
                                    placeholder="1"
                                >
                                <div class="form-text">Maximum 1000</div>
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/payment/links" class="btn btn-outline-secondary me-md-2">
                            <i class="fas fa-list me-1"></i>
                            View My Links
                        </a>
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="fas fa-link me-2"></i>
                            Generate Payment Link
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Information Card -->
        <div class="card mt-4">
            <div class="card-header">
                <h5 class="mb-0">
                    <i class="fas fa-info-circle me-2"></i>
                    How it works
                </h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4 text-center mb-3">
                        <div class="text-primary mb-2">
                            <i class="fas fa-edit fa-2x"></i>
                        </div>
                        <h6>1. Fill Details</h6>
                        <p class="text-muted small">Enter your product details and pricing information</p>
                    </div>
                    <div class="col-md-4 text-center mb-3">
                        <div class="text-success mb-2">
                            <i class="fas fa-link fa-2x"></i>
                        </div>
                        <h6>2. Generate Link</h6>
                        <p class="text-muted small">We create a secure Stripe payment link for you</p>
                    </div>
                    <div class="col-md-4 text-center mb-3">
                        <div class="text-info mb-2">
                            <i class="fas fa-share fa-2x"></i>
                        </div>
                        <h6>3. Share & Collect</h6>
                        <p class="text-muted small">Share the link and start collecting payments</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    </main>

    <footer class="bg-light mt-5 py-4">
        <div class="container text-center">
            <p class="text-muted mb-0">
                <i class="fas fa-shield-alt me-1"></i>
                Secure payment processing powered by Stripe
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" 
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" 
        crossorigin="anonymous"></script>
    <script src="/js/app.js"></script>
</body>
</html>
